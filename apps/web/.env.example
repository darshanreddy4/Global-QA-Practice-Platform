# Copy to .env.local for local dev overrides. Not needed if the API is served
# from the same domain under /api (the default, recommended setup).

# Only set this if the API is hosted on a DIFFERENT domain than the frontend,
# e.g. VITE_API_BASE_URL=https://your-api.onrender.com/api
# VITE_API_BASE_URL=/api
