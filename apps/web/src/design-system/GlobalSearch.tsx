// Moved to features/search/GlobalSearch.tsx (needs registry/domain data, which
// design-system/ must never import). Re-exported here only to avoid a dangling file.
export { GlobalSearch } from "../features/search/GlobalSearch";
